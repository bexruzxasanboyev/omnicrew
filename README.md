# OmniCrew

Marketing site for OmniCrew, an AI crew of custom robot agents.

```bash
cd omnicrew
npm install
npm run dev
```

Open the local URL Vite prints. Generated stills live in `public/images`. Generated loops live in `public/video` (mp4 + gif). Extra video prompts are in `public/prompts/VIDEO-PROMPTS.md`.
